# Results directory

Committed files are machine-readable benchmark summaries such as `doctor.json` and `bench_*.json`. Generated WAV files, full raw logs, model weights, caches, and credentials are ignored. Paths in summaries point to local artifacts and are not distribution assets.
